version: '3'
services:
    php:
        build:
            context: ./apache
        image: php:apache
        restart: always
        ports:
            - 80:80
        volumes:
            - ./../:/var/www/html:z
